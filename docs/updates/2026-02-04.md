# 2026-02-04 更新履歴

## 新機能追加

- タクソノミー・URL・ディレクトリ構造を一覧表示するドキュメント `docs/TAXONOMY_OVERVIEW.md` を追加
  - マーメイドダイアグラムでカテゴリ階層を可視化
  - カテゴリ別ページ一覧テーブル
  - タクソノミー定義一覧
  - 自動生成ファイル（手動編集禁止）

- タクソノミー概要を自動生成するスクリプト `apps/web/scripts/generate-taxonomy-overview.ts` を追加
  - `page-registry.json` と `taxonomy-classifier.ts` から自動生成
  - `npm run generate:taxonomy` で実行可能
